node_modules/
dist/
.env
.env.local
.env.development
.env.staging
.env.production
*.log
coverage/
.nyc_output/
.DS_Store
graphify-out/.graphify_python

# Local-disk storage driver (assessment PDFs etc. when STORAGE_DRIVER=local).
# Anchored to repo root so it does NOT match src/core/storage/ source code.
/storage/
